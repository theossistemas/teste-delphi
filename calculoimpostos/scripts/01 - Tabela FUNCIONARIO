CREATE TABLE FUNCIONARIO (
	ID		INTEGER			NOT NULL,
	NOME	VARCHAR(200)	NOT NULL,
	CPF		VARCHAR(11)		NOT NULL,
	SALARIO	NUMERIC(15,2)	NOT NULL
)

ALTER TABLE FUNCIONARIO ADD CONSTRAINT
PK_FUNCIONARIO PRIMARY KEY (ID)

CREATE GENERATOR GEN_ID_FUNCIONARIO